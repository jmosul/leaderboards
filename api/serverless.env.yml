offline:
  ENVIRONMENT: offline
  LEAGUE_POOL: default
  TABLE_PREFIX: leaderboards-offline

dev:
  ENVIRONMENT: dev
  LEAGUE_POOL: default
  TABLE_PREFIX: leaderboards-dev

production:
  ENVIRONMENT: production
  LEAGUE_POOL: default
  TABLE_PREFIX: leaderboards-production
